declare namespace FrontFredao {
    export type UserInfo = {
        username: string,
        password: string,
    }
    
    export type APIResponse = {
        status: number,
        message: string,
    }
}